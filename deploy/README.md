# Deploy env files

- `railway.env` — real values for Railway (gitignored). Open and paste into Railway Variables.
- `railway.env.example` — safe template (committed).

Secrets `JWT_ACCESS_SECRET` and `ENCRYPTION_KEY` must match Supabase Edge Function secrets.
